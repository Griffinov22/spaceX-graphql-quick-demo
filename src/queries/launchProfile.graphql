query LaunchProfile($id: ID!) {
  launch(id: $id) {
    id
    mission_name
    launch_date_local
    launch_site {
      site_name
    }
    rocket {
      rocket_name
      rocket_type
    }
    details
    telemetry {
      flight_club
    }
  }
}
